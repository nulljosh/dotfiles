{
  "manifest_version": 3,
  "name": "pwnlingo",
  "version": "1.0",
  "description": "Re-injects duo.js + boot.js on every lesson page load so the loop needs no operator.",
  "permissions": ["scripting", "tabs"],
  "host_permissions": ["https://www.duolingo.com/*"],
  "background": { "service_worker": "ext.js" }
}
