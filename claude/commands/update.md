---
description: Run all system and package manager updates
---

!`brew update; brew upgrade; brew upgrade --cask; brew doctor; gem update; npm upgrade -g; claude update; rbenv install -l | tail -1; fnm install --lts; fnm default lts-latest; true`
