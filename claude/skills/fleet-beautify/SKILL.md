---
name: fleet-beautify
description: Fleet-wide code-beauty sweep across ~/Documents/Code — junk files, dead code, debug leftovers, style noise. Use when the user asks to clean up/beautify "the whole codebase", "all my apps", or invokes /fleet-beautify.
---

# fleet-beautify

Alias for `/lint` run in fleet-wide mode. Invoke the `lint` skill and follow its
"Fleet-wide invocation" section — survey-first across every repo under
`~/Documents/Code`, same excludes, same rules. Don't duplicate the logic here.
